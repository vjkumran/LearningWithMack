# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'learnWithMAc' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for learnWithMAc
	platform :ios, '7.0'
	pod 'Canvas', '~> 0.1'
	pod 'Firebase/AdMob'
end
